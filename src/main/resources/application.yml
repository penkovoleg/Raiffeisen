server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://db-mysql:3306/socks
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  liquibase:
    change-log: classpath:liquibase/changelog.yml
    enabled: true